# URL du proxy Cloudflare Worker pour les API sans CORS (PVGIS).
# Laisser vide tant que le module Soleil/PV n'est pas branché.
VITE_PROXY_BASE=

# URL de la page héliodon historique (extrusion réelle, ombres portées,
# orthophoto, LiDAR). Par défaut : celle du repo esope023/Analyse.
VITE_LEGACY_HELIODON_URL=https://esope023.github.io/Analyse/heliodon.html
